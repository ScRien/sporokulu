<h1 class="mb-4">Ayarlar</h1>

{{#if error}}
<div class="alert alert-danger">{{error}}</div>
{{/if}}

{{#if success}}
<div class="alert alert-success">{{success}}</div>
{{/if}}

<h4 class="mt-4 mb-3">Şifre Değiştir</h4>

<form method="POST" action="/admin/settings/password" class="mb-5">

  <div class="mb-3">
    <label class="form-label">Mevcut Şifre</label>
    <input type="password" name="currentPassword" class="form-control" required>
  </div>

  <div class="mb-3">
    <label class="form-label">Yeni Şifre</label>
    <input type="password" name="newPassword" class="form-control" required>
  </div>

  <button class="btn btn-primary w-100">Şifreyi Güncelle</button>
</form>

<h3 class="mb-3">📄 Makbuz İşlemleri</h3>

<form method="POST" action="/admin/settings/regenerate-all-receipts">
  <button class="btn btn-danger">
    🔄 Tüm Makbuzları Yeniden Oluştur
  </button>
</form>